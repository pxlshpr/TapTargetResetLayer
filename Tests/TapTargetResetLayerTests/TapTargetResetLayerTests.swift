import XCTest
@testable import TapTargetResetLayer

final class TapTargetResetLayerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(TapTargetResetLayer().text, "Hello, World!")
    }
}
